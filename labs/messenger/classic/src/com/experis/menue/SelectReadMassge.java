package com.experis.menue;

import com.experis.sourse.FileReaders;

import java.util.Scanner;

import static java.lang.System.exit;

class SelectReadMassge {
    /*
    private String filePath;

    public SelectReadMassge(String filePath) {
        this.filePath = filePath;
    }

     String readMassge() {
        System.out.println("How you would like to send a message");
        System.out.println("1. Console");
        System.out.println("2. File");
        System.out.println("2. No want to send massge");
        Scanner scanner = new Scanner(System.in);
        try {
            int select = scanner.nextInt();

            switch (select) {
                case 1: {
                    return FileReaders.read(filePath);
                }
                case 2: {
                    return ReadFromConsole.read();
                }
                case 3: {
                    exit(0);
                }
                default: {
                    System.out.println("you need enter only 1/2/3");
                    break;
                }
            }
        } catch (IllegalArgumentException e) {
            System.out.println("you need enter only number");
        }
        return null;
    }

     */

}
