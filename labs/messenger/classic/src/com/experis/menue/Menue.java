package com.experis.menue;
import java.util.function.Predicate;


public class Menue {
/*
    String filePathToRead;
    String filePathToWrith;
    SelectReadMassge selectReadMassge;
    SelectTransform selectTransform;
    SelectWrith selectWrith;
    Predicate<String> condition;

    public Menue(String filePathToRead,String filePathToWrith, Predicate<String> condition) {
        this.filePathToRead = filePathToRead;
        this.filePathToWrith = filePathToWrith;
        selectReadMassge=new SelectReadMassge(filePathToRead);
        this.condition=condition;
    }

    public void run() {
        while (true) {
            String massge= selectReadMassge.readMassge();
            selectTransform=new SelectTransform(condition,massge);
            selectWrith=new SelectWrith(massge,filePathToWrith);
            selectWrith.selectWrith();

        }
    }


 */

}