connection.jdbc=jdbc:sqlite:
connection.dbFile=./data/chinook.sqlite

queries.idData=select FirstName||' '||LastName as name,Email,City from customers where CustomerId=?;
queries.albums= select AlbumId,Title from albums;
queries.track= select TrackId,name from tracks where AlbumId=?;